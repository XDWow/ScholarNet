// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"github.com/XD/ScholarNet/cmd/search/events"
	"github.com/XD/ScholarNet/cmd/search/grpc"
	"github.com/XD/ScholarNet/cmd/search/ioc"
	"github.com/XD/ScholarNet/cmd/search/repository"
	"github.com/XD/ScholarNet/cmd/search/repository/dao"
	"github.com/XD/ScholarNet/cmd/search/service"
	"github.com/google/wire"
)

// Injectors from wire.go:

func Init() *App {
	client := ioc.InitESClient()
	userDAO := dao.NewUserElasticDAO(client)
	userRepository := repository.NewUserRepository(userDAO)
	anyDAO := dao.NewAnyElasticDAO(client)
	anyRepository := repository.NewAnyRepository(anyDAO)
	articleDAO := dao.NewArticleElasticDAO(client)
	tagDAO := dao.NewTagElasticDAO(client)
	articleRepository := repository.NewArticleRepository(articleDAO, tagDAO)
	syncService := service.NewSyncService(userRepository, anyRepository, articleRepository)
	syncServiceServer := grpc.NewSyncServiceServer(syncService)
	searchService := service.NewSearchService(userRepository, articleRepository)
	searchServiceServer := grpc.NewSearchService(searchService)
	clientv3Client := ioc.InitEtcdClient()
	loggerV1 := ioc.InitLogger()
	server := ioc.InitGRPCxServer(syncServiceServer, searchServiceServer, clientv3Client, loggerV1)
	saramaClient := ioc.InitKafka()
	articleConsumer := events.NewArticleConsumer(saramaClient, loggerV1, syncService)
	userConsumer := events.NewUserConsumer(saramaClient, loggerV1, syncService)
	anyConsumer := events.NewAnyConsumer(saramaClient, loggerV1, syncService)
	v := ioc.NewConsumers(articleConsumer, userConsumer, anyConsumer)
	app := &App{
		server:    server,
		consumers: v,
	}
	return app
}

// wire.go:

var thirdProvicer = wire.NewSet(ioc.InitESClient, ioc.InitLogger, ioc.InitKafka, ioc.InitEtcdClient)

var serviceProviderSet = wire.NewSet(dao.NewUserElasticDAO, dao.NewArticleElasticDAO, dao.NewAnyElasticDAO, dao.NewTagElasticDAO, repository.NewUserRepository, repository.NewArticleRepository, repository.NewAnyRepository, service.NewSearchService, service.NewSyncService)
